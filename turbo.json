{
  "$schema": "https://turborepo.com/schema.json",
  "tasks": {
    "build": {
      "outputs": [
        ".next/**",
        "!.next/cache/**"
      ],
      "env": [
        "DB_DATABASE_URL",
        "BLOB_READ_WRITE_TOKEN",
        "UI_EDITABLE"
      ]
    },
    "lint": {}
  }
}